import { DATABASE_CONSTANTS } from './database.constants';
import { TENANT_CONSTANTS } from './tenant.constants';
import { VALIDATION_CONSTANTS } from './validation.constants';

export const CONSTANTS = {
  DATABASE: DATABASE_CONSTANTS,
  TENANT: TENANT_CONSTANTS,
  VALIDATION: VALIDATION_CONSTANTS,
};
